export interface Quotes {
  quoteText: string;
  quoteAuthor: string;
  buttonText: string;
  iconTwitter: any;
  onClickTwitter?: React.MouseEventHandler;
  onClickQuote?: React.MouseEventHandler;
}
